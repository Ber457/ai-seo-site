{
  "name": "ai-seo-site",
  "version": "1.0.0",
  "description": "Starter scaffold for AI SEO site with Claude content generation",
  "license": "MIT",
  "scripts": {
    "generate-monthly-content": "node scripts/generate-monthly-content.mjs",
    "build-site": "node scripts/build-site.mjs",
    "deploy": "node scripts/deploy.mjs",
    "cleanup": "node scripts/cleanup.mjs",
    "auto:deploy": "node -r dotenv/config ./scripts/solid-generate.mjs --per=2 && node ./scripts/sync-generated.mjs && npm run deploy"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "glob": "^11.0.3",
    "marked": "^16.1.2"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.60.0",
    "dotenv": "^17.2.1"
  }
}
